<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: DejaVu Sans, sans-serif; }
        h1 { color: #333; }
    </style>
</head>
<body>
<h1>Client {{ clientName }}</h1>
<table>
    <thead>
    <tr>
        <th>Date</th>
        <th>License type</th>
        <th>Product ID</th>
    </tr>
    </thead>
    <tbody>
    {% for licenseLog in licensesLog %}
        <tr>
            <td>{{ licenseLog.date | date('d/m/Y') }}</td>
            <td>{{ licenseLog.licenseType.value }}</td>
            <td>{{ licenseLog.productId }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>